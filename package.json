{
  "name": "reclip-downloader",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10",
  "engines": {
    "node": "24.x",
    "npm": "10.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @reclip/api\" \"npm run dev -w @reclip/worker\" \"npm run dev -w @reclip/web\"",
    "build": "npm run build -w @reclip/shared && npm run build -w @reclip/api && npm run build -w @reclip/worker && npm run build -w @reclip/web",
    "test": "vitest run"
  },
  "dependencies": {
    "@types/fluent-ffmpeg": "^2.1.3",
    "@types/node": "^25.7.0",
    "@types/node-fetch": "^2.6.13",
    "fluent-ffmpeg": "^2.1.3",
    "node-fetch": "^3.3.2",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "vitest": "^2.1.1"
  }
}
